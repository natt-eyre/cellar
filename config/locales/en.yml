en:
  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: Cellar

  products:
    index:
      add: Add an item
    new:
      add: Add to my cellar

  activerecord:
    models:
      product: Product
    attributes:
      product:
        name: Name
        quantity: Quantity
        expiry_date: Expiry Date!
